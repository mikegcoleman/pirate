# LLM API Environment Variables

# Base URL for the LLM API service (required)
# This should point to your model runner service
LLM_BASE_URL=http://model-runner.docker.internal/engines/v1

# Port for the Flask server to run on (optional, defaults to 8080)
PORT=8080

# Debug mode for Flask (optional, defaults to false)
# Set to "true" to enable debug mode
DEBUG=false 