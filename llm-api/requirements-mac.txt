# Mac-specific requirements (ElevenLabs TTS only)
# Use this for native Mac development: pip install -r requirements-mac.txt

Flask==2.3.3
requests==2.31.0
python-dotenv==1.0.0
elevenlabs

# Note: Kokoro TTS excluded for Mac - use TTS_PROVIDER=elevenlabs
# soundfile excluded as it's only needed for Kokoro TTS